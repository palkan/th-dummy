<% if @answer.valid? %>
  $("div#<%= dom_id @answer %>").html(
    '<%= j render partial: @answer, locals: { answer: @answer } %>'
  );
<% else %>
  $('<%= j render 'answers/validation_errors' %>').insertBefore($('.edit-answer-form form'));
<% end %>
